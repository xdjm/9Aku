<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="@dimen/layout_authorpaddingleft"
    android:paddingEnd="@dimen/layout_magin_x"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/layout_authorPB"
    app:layout_collapseMode="parallax">
    <TextView
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:textColor="@android:color/white"
        android:textSize="@dimen/textSize_x"
        android:layout_alignParentStart="true"
        android:id="@+id/location"
        android:layout_width="250dp"
        android:layout_height="wrap_content" />

    <TextView
        android:textColor="@android:color/white"
        android:textSize="@dimen/textSize_x"
        android:layout_below="@+id/location"
        android:id="@+id/publicrepos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:textColor="@android:color/white"
        android:layout_toEndOf="@+id/publicrepos"
        android:textSize="@dimen/textSize_x"
        android:layout_below="@+id/location"
        android:id="@+id/publicgists"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</RelativeLayout>
